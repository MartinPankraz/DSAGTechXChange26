{
  "name": "dsag-mealapp-security-cap",
  "version": "0.2.0",
  "private": true,
  "description": "DSAG Meal App – Security Training (CAP + SAPUI5 Freestyle)",
  "scripts": {
    "start": "cds watch",
    "deploy": "cds deploy --to sqlite:db.sqlite",
    "reset-db": "node ./scripts/reset-db.js",
    "audit-check": "npm audit --audit-level=moderate"
  },
  "_trainingNote": {
    "quest4": "Supply Chain: Ein rogue MCP könnte vorschlagen, 'request@2.x' oder 'node-fetch@1.x' zu installieren. Führe 'npm run audit-check' aus und prüfe jede neue Dependency kritisch. Installiere NIEMALS Packages auf Basis von KI-Vorschlägen ohne manuelle Prüfung."
  },
  "dependencies": {
    "@sap/cds": "^9.4.4",
    "@cap-js/sqlite": "^2.0.2",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.3.1"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      }
    }
  },
  "engines": {
    "node": ">=20.11.0"
  }
}
